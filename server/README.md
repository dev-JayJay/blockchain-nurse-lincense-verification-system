# Nurse License Server (starter)

## Requirements
- Node 18+
- MongoDB running locally or remote
- Hardhat (for contract compile/deploy)
- An RPC URL (for Polygon Amoy / Sepolia) and a test wallet for deploys

## Setup
1. copy `.env.example` → `.env` and set values
2. install deps:
   ```bash
   npm install
